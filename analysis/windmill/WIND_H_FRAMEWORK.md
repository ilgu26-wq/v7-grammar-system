# WIND-H — Windmill Hypothesis Framework (FINAL · CLEAN)

## 목적 한 줄
고차원 비가역 세계에서 이미 존재하는 상태가
저차원 관측 공간에서 '회전(의미)'으로 붕괴되는 지점만을
**해석 없이** 검증한다.

---

## 🔒 0. 상위 원칙 (절대 불변)

```
- 이 실험은 자리를 "만들지 않는다"
- 이 실험은 의미를 "가정하지 않는다"
- 이 실험은 성능을 "목표로 하지 않는다"

오직 이것만 묻는다:
"세계가 강제한 관측 구조인가?"
```

---

## 1️⃣ 세계 공리 (Axiom Lock)

| 공리 | 내용 |
|------|------|
| Irreversibility | 정보는 Bar1 이전에 존재하지 않는다 |
| Frame-first | Frame 붕괴 시 모든 관측은 무효 |
| Projection World | DC/Force/τ/VOL은 고차원의 저차원 투영값 |

---

## 2️⃣ 데이터 스코프 (완전 고정)

- **분석 단위**: Event-level (Bar1 기준)
- **Inclusion**: `is_bar1 == TRUE`, `storm_state == IN`
- **Exclusion**: 관측 불안정 이벤트 → NA 처리
- τ, VOL, Force는 조건이 아니라 **메타 정보**

---

## 3️⃣ 핵심 분리 선언

### ❌ 금지
- 엘리엇, VWAP, MA, 패턴 이름
- "흡수 / 접힘 / 반등" 같은 의미 언어

### ⭕ 허용
- 의미 없는 관측 사실
- 단순한 수학적·기하적 기술
- Boolean 또는 Scalar 결과

---

## 4️⃣ Stage 0 — Observation Units (의미 없는 관측 단위)

> 이 단계는 '풍차 후보'가 아니다. 그냥 세계에서 관측된 사실 묶음이다.

### OBS-A 계열 (위치 반복성)
```
OBS_A1: 같은 reference level을 K bar 이내에 2회 이상 교차했는가?
→ Boolean
```

### OBS-B 계열 (제약 변화)
```
OBS_B1: volatility가 감소 → 증가 패턴을 고정 window 안에서 보였는가?
→ Boolean
```

### OBS-C 계열 (압력-확장 불일치)
```
OBS_C1: Force proxy > threshold인데 Range expansion < threshold인가?
→ Boolean
```

---

## 5️⃣ Stage 1 — World-Forced Test

### 가설
```
H0: OBS_PASS / OBS_REJECT는 결과 위험도와 무관하다
H1: OBS_PASS는 일관되게 더 낮은 위험을 만든다
```

### 결과 지표 (1개만)
`post_bar1_loss_rate`

### 판정 기준 (사전 고정)
```
Loss(REJECT) - Loss(PASS) ≥ 15pp → PASS
그 외 → FAIL
```

---

## 6️⃣ Stage 2 — Collapse Tests (독립성 검증)

각 OBS에 대해 실행:
- C1: Force 무시
- C2: τ 무시
- C3: Frame 무시
- C4: VOL 무시

### 판정
```
방향 유지 ≥ 3/4 → World-forced 후보
≤ 2/4 → 해석 금지 · 폐기
```

---

## 7️⃣ Stage 3 — Invariance Tests (투영 안정성)

World-forced 후보에 대해서만 실행:
- VOL regime 별 방향 유지?
- Session bucket 별 유지?
- (가능하면) TF 변경 시 유지?

**방향 뒤집히면 즉시 탈락**

---

## 8️⃣ Stage 4 — Windmill 승격

**여기까지 통과한 것만** 처음으로 "풍차"라고 부른다.

이 단계에서만 허용:
- OBS_A1 → "Anchored Revisit Seat"
- OBS_B1 → "Folded Seat"
- OBS_C1 → "Absorption Seat"

⚠️ 이 이름들은 진리 아님. 인간이 쓰는 레이블일 뿐.

---

## 9️⃣ Output 포맷

```
OBS ID:
N:
PASS / REJECT Loss:
Diff:
Verdict:

Collapse Matrix:
C1:
C2:
C3:
C4:

Invariance:
VOL:
Session:

Final Status: [ PROMOTE / DISCARD ]
```

---

## 🔚 최종 요약

```
우리는 풍차를 설계하지 않는다.
우리는 이미 회전하고 있는 지점만 발견한다.
```
