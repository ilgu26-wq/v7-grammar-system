# Core Decision Grammar (Immutable)

This directory defines the immutable execution grammar of the V7 system.

Files in this directory:
- contain no predictive logic
- allow no parameter tuning
- are independent of market regime

All alpha, forecasting, or optimization logic
must exist outside this directory.

Once validated, this layer is frozen by design.
